# Benchmark report (local synthetic)

## Scenario
- Date: 2026-04-24
- Generator backend: `extractive`
- Embedding backend: `hash` (offline deterministic)
- Tool: `python scripts/run_benchmark.py` (FastAPI `TestClient` synthetic benchmark)
- Workload mix: 20 `/ingest`, 200 `/query`, 50 `/health` (plus 1 seed ingest)

## Reproducible command
```bash
python scripts/run_benchmark.py
cat artifacts/benchmark_summary.json
```

## Sample results (local synthetic only)
- p50 query latency: **11.6 ms**
- p95 query latency: **15.03 ms**
- error rate: **0.0**
- requests/sec: **89.69**

Source: `artifacts/benchmark_summary.json` generated by the command above.

## Environment assumptions
- Local process benchmark (not a distributed deployment)
- Offline-safe config (`EMBEDDING_BACKEND=hash`, `GENERATOR_BACKEND=extractive`)

## Limitations
- This is **not production performance**.
- Results do not include network hops, ingress controllers, autoscaling warm-up, or external LLM latencies.
